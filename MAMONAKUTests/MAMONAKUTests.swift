import Quick
import Nimble

final class MAMONAKUSmokeSpec: QuickSpec {
    override class func spec() {
        describe("Quick/Nimble smoke") {
            it("works") {
                expect(1 + 1).to(equal(2))
            }
        }
    }
}
